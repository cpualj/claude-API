{
  "name": "claude-worker",
  "version": "1.0.0",
  "type": "module",
  "description": "Claude CLI worker node for processing requests",
  "main": "worker.js",
  "scripts": {
    "start": "node worker.js",
    "dev": "nodemon worker.js",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "vitest": "^1.0.4",
    "@vitest/coverage-v8": "^1.0.4"
  }
}